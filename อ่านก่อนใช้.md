# ระบบแปะแพตช์ไทยออนไลน์ — คู่มือ

## วิธีใช้งาน / เผยแพร่
โฟลเดอร์นี้คือเว็บไซต์แบบ static (ไม่มี backend) ประกอบด้วย
- `index.html` — หน้าเว็บทั้งหมด
- `patches/` — ไฟล์แพตช์จริงทั้ง 3 เกม

**สำคัญ:** ต้องเปิดผ่าน HTTP/HTTPS จริง ห้ามดับเบิลคลิกเปิดไฟล์ตรงๆ (`file://`)
เพราะเบราว์เซอร์บล็อกการ `fetch()` ไฟล์ในเครื่องแบบนั้น

**ทดสอบในเครื่อง:**
```
cd webpatcher
python -m http.server 8000
```
แล้วเข้า `http://localhost:8000`

**เผยแพร่จริง (ฟรี):** อัปโหลดทั้งโฟลเดอร์ขึ้น GitHub Pages, Netlify, หรือ Cloudflare Pages
ได้เลย ไม่ต้องมีเซิร์ฟเวอร์หรือฐานข้อมูล เพราะการแปะแพตช์ทั้งหมดทำงานในเบราว์เซอร์ของผู้ใช้

## หลักการทำงาน
ไฟล์ ISO ต้นฉบับของผู้ใช้**ไม่ถูกอัปโหลดขึ้นเซิร์ฟเวอร์เลย** — ทุกอย่างทำในเบราว์เซอร์ผ่าน
JavaScript (File API + Web Crypto สำหรับ SHA-256) ระบบจะ:
1. อ่านไฟล์ ISO ที่ผู้ใช้ลากมา
2. ตรวจ SHA-256 ว่าตรงกับต้นฉบับที่รองรับ
3. โหลดไฟล์แพตช์ (ที่โฮสต์อยู่ในเว็บนี้) แล้วแปะทับในหน่วยความจำ
4. ตรวจ SHA-256 ของผลลัพธ์อีกครั้งก่อนให้ดาวน์โหลด

## รูปแบบแพตช์ที่รองรับ และสถานะการตรวจสอบ

| เกม | รูปแบบ | วิธีถอดรหัสในเบราว์เซอร์ | สถานะ |
|---|---|---|---|
| Queen's Gate | QGDELTA1 (custom, เก็บชิ้นส่วนแทนที่แบบไม่บีบอัดใน zip) | เขียนเอง (byte-copy ล้วนๆ) + ใช้ JSZip อ่านไฟล์ zip | ✅ ตรวจสอบกับไฟล์จริงแล้วในเครื่องมือพัฒนา (parse ตรงกับสคริปต์ .ps1 ต้นฉบับทุกจุด) |
| Sol Trigger | STPATCH1 (custom, บล็อก zlib-deflate ตาม offset) | เขียนเอง ใช้ `DecompressionStream('deflate')` ของเบราว์เซอร์ (รองรับ Chrome/Edge/Firefox รุ่นใหม่) | ✅ ตรวจสอบกับไฟล์จริงแล้ว ถอดรหัสได้ครบทั้ง 17 บล็อกตรงกับที่คาดไว้ |
| Tales of Phantasia | XDELTA มาตรฐาน (VCDIFF ของ xdelta3 พร้อม secondary compression) | ใช้ไลบรารี `xdelta3-wasm` (คอมไพล์ตัว xdelta3 ของจริงเป็น WASM) โหลดผ่าน CDN | ⚠️ **ยังไม่ได้ทดสอบรันจริงแบบ end-to-end** เพราะสภาพแวดล้อมที่ใช้เตรียมไฟล์นี้ไม่มีอินเทอร์เน็ต/เบราว์เซอร์ให้รันทดสอบ โค้ดเขียนตาม API ที่ไลบรารีนี้ประกาศไว้ (`xd3_decode_memory`) แต่ควรทดสอบกับไฟล์จริงก่อนเผยแพร่ ถ้าใช้ไม่ได้ ทางเลือกสำรองคือให้ผู้ใช้เกมนี้ใช้วิธีเดิม (ดับเบิลคลิก .bat + xdelta3.exe) ไปก่อน

ไฟล์แพตช์ทุกไฟล์มีการตรวจ SHA-256 ทั้งก่อนและหลังแปะเสมอ — ถ้าถอดรหัสผิดพลาด
ระบบจะไม่ปล่อยไฟล์ผลลัพธ์ที่เสียหายออกมาให้ดาวน์โหลด (แค่ขึ้น error)

## ข้อจำกัดที่ควรรู้
- ไฟล์ ISO ขนาดใหญ่ (500MB–1.5GB) จะถูกโหลดทั้งไฟล์เข้าหน่วยความจำเบราว์เซอร์ — เครื่องที่ RAM
  น้อยหรือใช้มือถือ อาจช้าหรือค้าง แนะนำให้ใช้บนคอมพิวเตอร์
- `DecompressionStream` รองรับเบราว์เซอร์ใหม่เท่านั้น (Chrome/Edge 80+, Firefox 113+, Safari 16.4+)
- ทุกไฟล์ยังคงมีลิขสิทธิ์ของเจ้าของเดิม — เว็บนี้แจกเฉพาะ "ไฟล์แพตช์" (ส่วนต่าง) ไม่ได้แจก ISO เกม
